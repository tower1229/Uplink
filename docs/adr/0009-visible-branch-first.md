# First version captures the visible branch only

The Message graph may represent branches and regenerations, but first-version Import and Capture are successful if they persist the currently visible branch. Exhaustive traversal of every side branch is deferred. Flattening to a single timeline was rejected so later versions can deepen branch coverage without rewriting the model.
